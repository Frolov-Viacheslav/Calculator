#!groovy
//properties([disableConcurrentBuilds()])

pipeline {
  agent{
    label 'master'
  }
 options {
    timestamps()
  }
  stages {
    stage('Build app') {
      steps {
       // sh '''
            //if  [ -d HelloWorld ]; then
           // rm -r HelloWorld
           // fi
           // git clone "git@github.com:Frolov-Viacheslav/HelloWorld.git"
           // ansible-playbook HelloWorld/Ansible/playbook.yml
          //  rm -r HelloWorld
            
          // '''
        sh 'ansible-pull -U git@github.com:Frolov-Viacheslav/HelloWorld.git -l jenkins Ansible/playbook.yml'
      }
    }
  }
}
